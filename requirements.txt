flight_reservation/
│
├── main.py
├── db.py
├── ui.py
├── utils.py
├── flights.db          ← optional: ignore or include
├── .gitignore
├── requirements.txt    ← list dependencies
├── /images/            ← UI icons (don’t ignore)
├── /__pycache__/       ← ignore
├── /venv/              ← ignore
└── /build/, /dist/     ← ignore if you package to .exe